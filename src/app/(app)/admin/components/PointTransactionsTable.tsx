"use client";

import React from 'react';
import type { PointTransaction } from '@/types';

const PointTransactionsTable = ({ transactions, loading }: { transactions: PointTransaction[], loading: boolean }) => {
    return <div>Point Transactions Table Placeholder</div>;
}

export default PointTransactionsTable;

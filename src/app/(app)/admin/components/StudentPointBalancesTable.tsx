"use client";

import React from 'react';
import type { User as StudentUser } from '@/types';

const StudentPointBalancesTable = ({ students, loading }: { students: StudentUser[], loading: boolean }) => {
    return <div>Student Point Balances Table Placeholder</div>;
}

export default StudentPointBalancesTable;
